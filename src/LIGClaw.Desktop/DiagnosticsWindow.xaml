<Window x:Class="LIGClaw.Desktop.DiagnosticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="문제 해결 정보" Width="820" Height="560" MinWidth="560" MinHeight="360"
        Icon="Assets/ligclaw.ico" WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Style="{StaticResource PageTitleStyle}" Text="문제 해결 정보" />
            <TextBlock Margin="0,5,0,0" Style="{StaticResource PageSubtitleStyle}"
                       Text="Sidecar 연결과 실행 상태를 시간순으로 확인할 수 있습니다." />
        </StackPanel>
        <ListBox x:Name="DiagnosticsList" Grid.Row="1" Margin="0,18,0,0"
                 ItemsSource="{Binding}" FontFamily="{DynamicResource MonoFontFamily}" FontSize="{DynamicResource TypeControlSize}"
                 HorizontalContentAlignment="Stretch"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 AutomationProperties.Name="문제 해결 로그">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Padding="4,2" Text="{Binding}" TextWrapping="NoWrap" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Style="{StaticResource CaptionTextStyle}"
                       Text="진단 번들은 설정 화면에서 저장할 수 있습니다." />
            <Button Grid.Column="1" Margin="0,0,9,0" Style="{StaticResource SecondaryButtonStyle}"
                    Click="CopyAll_Click" Content="전체 복사" />
            <Button Grid.Column="2" Click="Close_Click" Content="닫기" />
        </Grid>
    </Grid>
</Window>
