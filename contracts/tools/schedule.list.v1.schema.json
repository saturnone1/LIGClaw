{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ligclaw.local/contracts/tools/schedule.list.v1.schema.json",
  "title": "schedule.list.v1",
  "description": "Lists durable local notification schedules after explicit user approval.",
  "type": "object",
  "additionalProperties": false,
  "required": ["reason"],
  "properties": {
    "includeInactive": { "type": "boolean" },
    "reason": { "type": "string", "minLength": 1, "maxLength": 160 }
  }
}
